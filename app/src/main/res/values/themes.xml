<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RecipeScroll" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/light_primary</item>
        <item name="android:textColor">#373737</item>
    </style>
    <style name="Theme.RecipeScroll.All" parent="Base.Theme.RecipeScroll">
        <item name="colorPrimary">@color/light_primary</item>
    </style>
    <style name="Theme.RecipeScroll.Header" parent="Base.Theme.RecipeScroll">
        <item name="android:fontFamily">@font/caveat_brush</item>
        <item name="android:textSize">20sp</item>
    </style>
    <style name="Theme.RecipeScroll.Body" parent="Base.Theme.RecipeScroll">
        <item name="android:fontFamily">@font/caveat</item>
        <item name="android:textSize">15sp</item>
    </style>
</resources>